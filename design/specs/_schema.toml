[_meta]
type        = "spec"
description = "Specification document schema"

[fields.id]
required = true
pattern  = "^SPEC-\\d{3}$"

[fields.title]
required = true

[fields.status]
required = true

[sections]
required = ["implements", "scope", "deliverables", "acceptance"]
